# Feature Implementation Specs

This folder stores implementation specs for individual features. Specs should be concrete enough for unattended implementation: scoped files, ordered steps, and focused validation after each step.

Use `agent/skills/spec/SKILL.md` when creating specs in this folder.

## File Naming

Use `short-feature-name.spec.md`.
